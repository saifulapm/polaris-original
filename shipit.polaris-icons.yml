ci:
  require:
    - 'Validate with Node v14.17.0'
    - 'Validate with Node v16.13.0'

merge:
  require:
    - 'Validate with Node v14.17.0'
    - 'Validate with Node v16.13.0'

dependencies:
  override:
    - pnpm install

deploy:
  override:
    - pnpm turbo run build --filter=@shopify/polaris-icons
    - npm publish polaris-tokens --tag latest
